# Core dependencies
discord.py>=2.3.0,<2.4.0
aiohttp>=3.9.0

# Optional but recommended
python-dotenv>=1.0.0  # For local development
psutil>=5.9.0  # For better system metrics
ujson>=5.8.0  # Faster JSON processing (if needed)

# Version pins for stability
certifi>=2024.0.0
charset-normalizer>=3.3.0
idna>=3.6
requests>=2.31.0
urllib3>=2.1.0
